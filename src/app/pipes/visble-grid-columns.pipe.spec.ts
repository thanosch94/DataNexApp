import { VisbleGridColumnsPipe } from './visble-grid-columns.pipe';

describe('VisbleGridColumnsPipe', () => {
  it('create an instance', () => {
    const pipe = new VisbleGridColumnsPipe();
    expect(pipe).toBeTruthy();
  });
});
