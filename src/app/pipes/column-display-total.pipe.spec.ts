/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ColumnDisplayTotalPipe } from './column-display-total.pipe';

describe('Pipe: ColumnDisplayTotale', () => {
  it('create an instance', () => {
    let pipe = new ColumnDisplayTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
