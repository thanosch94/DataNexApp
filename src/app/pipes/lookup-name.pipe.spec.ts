import { LookupNamePipe } from './lookup-name.pipe';

describe('LookupNamePipe', () => {
  it('create an instance', () => {
    const pipe = new LookupNamePipe();
    expect(pipe).toBeTruthy();
  });
});
